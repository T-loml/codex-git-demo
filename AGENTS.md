# Project Instructions

- Build Vue code using official Vue 3 patterns and APIs.
- Prefer Single-File Components with `<script setup>` for new components.
- Use semantic variable, function, prop, and component names.
- Keep component responsibilities focused and easy to scan.
- Add concise comments above important code blocks when they clarify intent.
- Avoid adding dependencies unless the task explicitly requires them.
